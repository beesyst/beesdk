# BeeSDK implementation and tests

Read `AGENTS.md` and `.agents/skills/beesdk-implementation-tests/SKILL.md`.

## Target

- Repository: `beesdk`
- Worktree: `<TARGET_WORKTREE>`
- Expected branch: `<FEATURE_BRANCH>`
- Base branch: `main`

## Approved Issue

```text
<FULL_APPROVED_ISSUE>
```

Implement only the approved scope. Preserve BeeSDK as a standalone, contracts-only package; do not require or modify a consumer project unless explicitly scoped. Run the required checks and return the full implementation and verification report. Do not commit, push, create a PR or merge.
