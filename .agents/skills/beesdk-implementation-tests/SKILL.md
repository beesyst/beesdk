# BeeSDK implementation and tests

Use this skill to implement an approved BeeSDK Issue in its exact target worktree.

Read `AGENTS.md`, the approved Issue, `docs/ROADMAP.md`, `docs/SDLC.md`, `docs/SECURITY.md` and relevant public contracts before editing. Keep the package standalone and stdlib-only at runtime unless the approved Issue explicitly changes that boundary. Do not perform consumer-project changes unless they are explicitly in scope.

Implement the smallest complete solution, add or update contract tests, and run checks required by the actual change level. Do not commit, push, open a PR or merge. Return the required implementation evidence from `AGENTS.md` with exact command results.
